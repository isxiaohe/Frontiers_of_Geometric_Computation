model: pointnext_s
num_points: 1024
num_classes: 40
batch_size: 32
epochs: 200
lr: 0.001
weight_decay: 0.05
optimizer: adamw
scheduler: cosine
grad_norm_clip: 1.0
label_smoothing: 0.2
use_normals: false
num_workers: 4
data_root: datasets/modelnet40_hdf5_2048
augment: pointnext

wandb:
  project: pointnext-modelnet40
  mode: offline
  name: pointnext_s
