model: pointnext_b
num_points: 1024
num_classes: 40
batch_size: 24
epochs: 600
lr: 0.001
weight_decay: 0.05
optimizer: adamw
scheduler: cosine
label_smoothing: 0.2
use_normals: false
num_workers: 4
data_root: datasets/modelnet40_hdf5_2048
augment: pointnext

wandb:
  project: pointnext-modelnet40
  mode: offline
  name: pointnext_b
